import javax.swing.*;

public class Success {
    public static void success() {
        JFrame hello = new JFrame();
        hello.setSize(1280,720);
        hello.setLocationRelativeTo(null);
        hello.setLayout(null);
    }
}
